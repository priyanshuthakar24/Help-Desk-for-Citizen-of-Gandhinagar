<%- include('../includes/head.ejs') %>
    </head>

    <body>
        <h1>
            Deparatment List
        </h1>
        <div class="main-container">
            <% for( let key of deptdata){%>
                <div class="options">
                    <form action="/deptinfo" method="post">
                        <button type="submit">
                            <%= key.name%>
                        </button>
                        <input type="hidden" value="<%= key.name%>" name="deptname">
                    </form>
                </div>
                <%}%>
        </div>
        <%- include('../includes/end.ejs') %>